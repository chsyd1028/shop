# server
# server
server.port=8080
#server.context-path=/shop/
#spring.application.name=shop
# mysql
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/shop?useSSL=false&useUnicode=true&characterEncoding=utf8&autoReconnect=true
spring.datasource.username=root
spring.datasource.password=Duoduo521.

# thymeleaf
spring.thymeleaf.prefix=classpath:/templates/view/
spring.thymeleaf.suffix=.html

# 友好的HTML5标准
spring.thymeleaf.mode=LEGACYHTML5

# mybatis
mybatis.mapper-locations=classpath:/templates/mapper/*Mapper.xml
mybatis.type-aliases-package=com.csyd.blog.model

# 自动转换成驼峰
mybatis.configuration.map-underscore-to-camel-case=true

# redis全家桶
spring.redis.host=localhost
spring.redis.port=6379
# 同时最大连接数
spring.redis.pool.max-active=64
# 最大空闲连接数
spring.redis.pool.max-idle=8
# 最大获取等待时间
spring.redis.pool.max-wait=5000

# 热部署
spring.devtools.livereload.enabled=true
